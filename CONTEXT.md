# 星拾 领域词汇表

本文件只定义词汇，不含实现细节。术语按「用户看到的东西」与「系统内部的概念」分组。

## 划词与翻译

### 选中范围 (Selection)
用户在网页上高亮的一段文字。同一时刻全页只有一个「当前选中范围」，新的划词会替换它。
系统在划词时额外记录**所在句子**（选中范围所在的那句完整句子），用于给翻译和助手提供语境。

### 触发图标 (Trigger Icon)
划词后出现在选区旁的圆形「译」按钮。它是**开关**：当前没有译文卡片时点击发起翻译；已有译文卡片时点击是**关闭**，不是重新翻译。

### 译文卡片 (Translation Card)
页面上承载翻译结果的浮层，全页**只有一个实例**，可拖动、可调尺寸、默认固定（固定态下滚动与点击页面不会关闭它）。
卡片有**两种模式**，同一实例内切换，不是两个窗口：
- **翻译模式**：展示一个词的译文与知识区（百科、释义、例句等）。
- **助手模式**：展示与 AI 的对话，会话在两种表面（卡片 / 侧栏）之间共享、不中断。

**易混淆**：卡片始终只有一张。历史上曾出现的「点翻译像关掉旧窗口再弹新的」，指的是卡片内容被替换，而非第二个窗口出现。

### 译文历史 (Translation History)
卡片记住的**最近查过的词**，可在卡片内前后回退。
- 只记录**划词翻译成功的结果**；同一「词 + 翻译源」重复翻译只保留最新一条，不产生重复项。
- 只存在于内存，刷新页面即清空；但**关闭卡片不清空**（滚动、点击页面、Esc、✕ 都不清空）。
- 回退是**直接回显已缓存的结果**，不重新请求翻译源。

**边界**：译文历史 ≠ 助手会话历史。前者是查过的词，后者是问过的问题，两者互不影响，回退译文不会改变对话。

### 翻译源 (Translator Source)
提供翻译结果的后端（百度、DeepL、Google、腾讯、DeepSeek 等）。同一段文字换源重译**不**在译文历史里新增条目。

### 知识区 (Knowledge Sections)
译文卡片里除译文外的结构化内容（百科、释义、词形变化、同反义词、常用搭配、词根词缀、易混辨析、记忆技巧、语域、例句），可按项配置显隐。

## AI 助手

### 助手设置 (Assistant Settings)
用户对助手行为的配置，跨页面持久保存。包含上下文长度、思考深度、回答长度上限、可编辑规则、预设提问、是否附带选中范围。

### 上下文长度 (Context Length)
随提问发送的页面正文字符上限。正文围绕**选中范围**开窗截取，而非固定取页面开头；取值为 0 表示只发送选中范围本身。

### 思考深度 (Thinking Depth)
助手在回答前进行推理的程度（关闭 / 低 / 高 / 最大）。关闭最快且最便宜；开启后按输出计费，且模型的 temperature 参数失效。

### 可编辑规则 (Editable Rules)
用户可改写的助手行为要求，注入系统提示词。
内置三条默认规则（优先依据页面内容、简体中文且简洁、解释外语词汇的方式）**允许用户修改**；另有一条**防提示词注入规则锁定不可编辑**——页面正文与选中范围都是待阅读的数据，不是对助手的指令。这是安全兜底，不交给用户关闭。

**边界**：可编辑规则取代了旧的「附加指令」。二者职责完全相同（都是用户写的助手行为要求），合并为单一入口，避免「我该在哪写规则」的歧义。

### 预设提问 (Preset Prompts)
助手输入框上方的快捷提问按钮（默认：解释选中 / 说人话 / 给例子 / 考考我 / 这页讲了什么）。用户可增、删、改、上下排序。
每条预设除标签与提示词外，还带两个行为属性：
- **是否需要选中范围**：需要时，页面上没有选中内容则该按钮禁用并提示先划词。
- **取上下文的方式**：围绕选中内容开窗，或从页面开头取（整页速览类提问用后者）。

预设由**归一化保证 id 唯一**：缺失或重复时补生成，使「按 id 定位预设」永远指向确定的一条。预设的渲染与分发都以 id 为键，因此 id 冲突会导致点 A 发出 B 的提问——这是必须由归一化堵死的静默失配。

补生成的 id 必须**由内容确定性地推出**（而非随机），因为归一化是纯函数、会在每次读盘时重跑；若 id 每次不同，设置对象永不稳定，前端也无法用 id 作为渲染键。

**整页速览**是预设提问的一种，但它渲染在输入框工具栏而非 chip 栏。工具栏那个按钮**从预设列表派生**（按行为属性筛选），不是独立的硬编码入口——用户删掉对应的预设，按钮随之消失。这样预设列表是唯一真源，不会出现「预设删了、按钮还在、点击静默失败」的失配。

**边界**：预设提问的**内容**（有哪些、叫什么、问什么）全部用户可改，包括整页速览类。不存在「锁定预设」的概念。

### 助手会话 (Assistant Session)
一轮轮的问答记录，包含展示消息与送给模型的消息。与译文历史相互独立。清空会话不影响译文历史，反之亦然。

### 页面上下文 (Page Context)
随提问发送的页面信息：标题、地址、最近的标题路径、截取的正文。它在**同一会话内必须保持逐字节稳定**，否则无法命中模型的前缀缓存（命中价约为未命中的 1/50）。因此它只在改设置、清空会话、页面地址变化、选中范围不再落在已缓存片段内时才重建。
