{
  "name": "translate-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "preview": "vite build --mode development && echo 'Load dist/ as unpacked extension in chrome://extensions'"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-web-extension": "^4.0.0"
  },
  "dependencies": {
    "lit": "^3.2.0"
  }
}
